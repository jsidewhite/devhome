<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyUserControl.MyUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyUserControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView ItemsSource="{x:Bind Employees}" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Employee">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind Id}" Margin="5"/>
                        <TextBlock Text="{x:Bind Name}" Margin="5"/>
                        <TextBlock Text="{x:Bind Age}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
