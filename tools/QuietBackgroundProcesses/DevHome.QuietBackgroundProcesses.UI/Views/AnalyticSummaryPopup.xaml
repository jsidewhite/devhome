<!-- Copyright (c) Microsoft Corporation.. -->
<!-- Licensed under the MIT License. -->

<ContentDialog
    x:Class="DevHome.QuietBackgroundProcesses.UI.Views.AnalyticSummaryPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevHome.QuietBackgroundProcesses.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}">

    <!-- ContentDialog Width and Height are not properly hooked up and must be set this way -->
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">652</x:Double>
        <x:Double x:Key="ContentDialogMaxWidth">652</x:Double>
        <x:Double x:Key="ContentDialogMaxHeight">590</x:Double>
        <Thickness x:Key="ContentDialogTitleMargin">0,0,0,0</Thickness>
        <Thickness x:Key="ContentDialogPadding">0,0,0,0</Thickness>
        <Thickness x:Key="NavigationViewContentMargin">0,0,0,0</Thickness>
        <Thickness x:Key="SmallPinButtonMargin">0,20</Thickness>
        <Thickness x:Key="LargePinButtonMargin">0,42</Thickness>
        <Thickness x:Key="SmallWidgetPreviewTopMargin">0,20,0,0</Thickness>
        <Thickness x:Key="LargeWidgetPreviewTopMargin">0,42,0,0</Thickness>
    </ContentDialog.Resources>

    <!-- Title and Close button -->
    <Grid x:Name="AddWidgetTitleBar22" Height="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="AddWidgetTitleBar" Padding="15">
            <TextBlock x:Uid="AddWidgetsTitle" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Left" Margin="16,10,0,0" Text="Analytic Summary"/>
            <TextBlock x:Uid="AddWidgetsTitle2" HorizontalAlignment="Left" Margin="16,10,0,0">
            <Run Text="Analytic summary description..." />
            <Hyperlink NavigateUri="https://aka.ms/dsc.yaml">
                <Run Text="open .json file" />
            </Hyperlink>
            </TextBlock>
            <!--<commonviews:CloseButton Command="{x:Bind CancelButtonClickCommand}" />-->
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" Padding="10 0 10 0">
            <StackPanel Padding="15">
                <StackPanel x:Name="AddWidgeffftTitleBar">
                    <!--<commonviews:CloseButton Command="{x:Bind CancelButtonClickCommand}" />-->
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="15,0,15,0">
                    <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Process" Width="240" />
                    <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Process type" Width="130" />
                    <TextBlock Foreground="{StaticResource TextFillColorSecondaryBrush}" Text="Cpu above threshold" Width="110" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <local:SimpleTableControl x:Name="processListControl" />

            </StackPanel>

        </ScrollViewer>

        <Grid x:Name="AddWidgetTitleBar22fwfe" Grid.Row="2" Background="{StaticResource SmokeFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button
                x:Name="SaveButton"
                x:Uid="PinButton"
                Width="Auto"
                Style="{ThemeResource AccentButtonStyle}"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Margin="20,20,5,20"
                MinHeight="32" MinWidth="118">
                <TextBlock FontSize="14" Text="Save report" />
            </Button>

            <Button
                x:Name="CloseButton"
                Grid.Column="1"
                x:Uid="CloseButton"
                Width="Auto"
                Click="CloseButton_Click"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center"
                Margin="5,20,20,20"
                MinHeight="32" MinWidth="118">
                <TextBlock FontSize="14" Text="Close" />
            </Button>
        </Grid>
    </Grid>

</ContentDialog>
